{
  "buildCommand": "npm install --prefix web-app && npm run app:build",
  "outputDirectory": "web-app/dist",
  "rewrites": [
    {
      "source": "/((?!assets/).*)",
      "destination": "/index.html"
    }
  ]
}
